// @flow
import * as React from 'react';
import TablePlaceholder from '~ds/Table/placeholder';

const QuestionnaireListPlaceholder = (): React.Node => (
  <TablePlaceholder rowsCount={10} columnsCount={5} />
);

export default QuestionnaireListPlaceholder;
