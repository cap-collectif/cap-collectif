import styled from 'styled-components'
import colors from '~/styles/modules/colors'

export const QuestionnaireHeaderContainer = styled.div`
  .wysiwyg-render {
    color: ${colors['neutral-gray']['900']};
    font-size: 18px;
    line-height: 24px;
    padding-left: 0 !important;
  }
`
