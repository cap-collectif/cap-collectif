// @flow
import styled, { type StyledComponent } from 'styled-components';

const StepPageHeaderContainer: StyledComponent<{}, {}, HTMLDivElement> = styled.div`
  .card {
    border: none;
    box-shadow: 0 0 3px 1px #ddd;
  }
`;

export default StepPageHeaderContainer;
