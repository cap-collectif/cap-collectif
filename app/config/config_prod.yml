imports:
  - { resource: config.yml }

framework:
  validation:
    cache: validator.mapping.cache.doctrine.apc

sentry:
  dsn: '%sentry_dsn%'
  options:
    environment: '%kernel.environment%'

monolog:
  handlers:
    console:
      type: console
      bubble: false
    nested:
      type: stream
      path: 'php://stderr'
      level: warning
    sentry:
      level: warning
      type: raven
      dsn: '%sentry_dsn%'
