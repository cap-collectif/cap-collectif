fos_elastica:
    serializer: ~
    # clients:
    #     default: { host: localhost, port: 9200 }
    # indexes:
    #     app:
    #         index_name: app_%kernel.environment%
                # types:
                    # user:
    #                     serializer:
    #                         groups: [elastica, Default]
