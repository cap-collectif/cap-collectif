hautelook_alice:
  # Locale to used for faker; must be a valid Faker locale otherwise will fallback to en_US
  locale: '%locale%'
  # Maximum number of time the loader will try to load the files passed
  loading_limit: 100
