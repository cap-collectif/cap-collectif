const initialState = {
  currentProjectStepById: null,
  currentProjectById: null,
  projects: [],
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
