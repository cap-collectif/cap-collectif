namespace :capco do
    desc 'recalculate counters'
    task :recalculate do
        invoke 'symfony:console', 'capco:recalculate-counters', '--no-interaction'
        invoke 'symfony:console', 'capco:recalculate-consultations-counters', '--no-interaction'
    end
end
