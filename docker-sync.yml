options:
  compose-file-path: 'infrastructure/environments/current.yml'
  verbose: false
  cli_mode: 'auto'

syncs:
  capco-sync:
    notify_terminal: true
    src: '.'
    dest: '/var/www'
    sync_host_ip: 'localhost'
    sync_strategy: 'rsync'
    sync_host_port: 10872
