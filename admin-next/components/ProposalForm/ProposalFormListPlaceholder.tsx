import type { FC } from 'react';
import TablePlaceholder from '../UI/Table/TablePlaceholder';

const ProposalFormListPlaceholder: FC = () => <TablePlaceholder rowsCount={10} columnsCount={5} />;

export default ProposalFormListPlaceholder;
