Capco\AppBundle\Entity\MapToken:
  mapboxMapToken:
    id: mapboxToken
    # TODO @spyl94 in next PR
    initialPublicToken: ***REMOVED***
    publicToken: ***REMOVED***
    secretToken: ***REMOVED***
    provider: MAPBOX
