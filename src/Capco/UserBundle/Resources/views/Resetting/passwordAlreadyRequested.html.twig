{% extends "CapcoUserBundle::layout.html.twig" %}
{% block title %} {{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }} {{ parent() }} {% endblock %}

{% block jumbotron %}
    <div class="jumbotron  jumbotron--custom  jumbotron--bg-1  text-center">
        <div class="container">
            <h1 class="h2  h--no-border">{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}</h1>
        </div>
    </div>
{% endblock %}

{% block fos_user_content %}
    <div class="container">
        <div class="alert alert-warning  block" role="alert">
            <p>{{ 'resetting.password_already_requested'|trans({}, 'FOSUserBundle') }}</p>
        </div>
    </div>
{% endblock %}
