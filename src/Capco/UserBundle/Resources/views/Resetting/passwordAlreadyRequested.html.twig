{% extends "CapcoUserBundle::layout.html.twig" %}
{% block title %} {{ 'resetting.request.title'|trans({}, 'CapcoAppBundle') }} {{ parent() }} {% endblock %}

{% block jumbotron %}
    <div class="jumbotron  jumbotron--custom  jumbotron--bg-1  text-center">
        <div class="container">
            <h1 class="h2  h--no-border">{{ 'global.submit'|trans({}, 'CapcoAppBundle') }}</h1>
        </div>
    </div>
{% endblock %}

{% block fos_user_content %}
    <section class="section--custom">
        <div class="container">
            <div class="alert alert-warning  block" role="alert">
                <p>{{ 'resetting.password_already_requested'|trans({}, 'CapcoAppBundle') }}</p>
            </div>
        </div>
    </section>
{% endblock %}
