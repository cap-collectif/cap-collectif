{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2>{{ 'resetting.email.subject'|trans({}, 'FOSUserBundle') }}</h2>
                <div class="alert alert-warning alert-dismissible  block" role="alert">
                    <p>{{ 'resetting.password_already_requested'|trans({}, 'FOSUserBundle') }}</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
