Capco\AppBundle\Entity\Event:
  attributes:
    id:
      groups: [Events, Elasticsearch]
    title:
      groups: [Events, Elasticsearch]
    body:
      groups: [Events, Elasticsearch]
    Author:
      serialized_name: author
      groups: [Events, ElasticsearchNestedAuthor]
    projects:
      groups: [ElasticsearchNestedProject]
    themes:
      groups: [Elasticsearch]
    canContribute:
      groups: [Events]
    createdAt:
      groups: [Events, Elasticsearch]
    updatedAt:
      groups: [Events]
    startAt:
      groups: [Default, Elasticsearch]
    endAt:
      groups: [Default, Elasticsearch]
    media:
      groups: [Default]
    comments:
      groups: [Events]
    isEnabled:
      serialized_name: enabled
      groups: [Events, Elasticsearch]
    address:
      groups: [Elasticsearch]
    link:
      groups: [Events]
