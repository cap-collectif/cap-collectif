Capco\AppBundle\Entity\Synthesis\Synthesis:
  attributes:
    id:
      groups: [Syntheses, SynthesisDetails]
    enabled:
      groups: [SynthesisDetails]
    editable:
      groups: [SynthesisDetails]
    elements:
      groups: [Syntheses, SynthesisDetails, Elements]
    displayRules:
      groups: [Syntheses, SynthesisDetails]
