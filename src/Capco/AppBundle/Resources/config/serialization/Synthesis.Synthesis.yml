Capco\AppBundle\Entity\Synthesis\Synthesis:
  attributes:
    id:
      groups: [Syntheses, SynthesisDetails]
    enabled:
      groups: [SynthesisDetails]
    editable:
      groups: [SynthesisDetails]
    elements:
      groups: [Syntheses, SynthesisDetails, Elements]
    displayRules:
      groups: [Syntheses, SynthesisDetails]
  relations:
    - rel: self
      href:
        route: get_synthesis
        parameters:
          id: expr(object.getId())
      exclusion:
        groups: [Syntheses, SynthesisDetails]
    - rel: elements
      href:
        route: get_synthesis_elements
        parameters:
          id: expr(object.getId())
      exclusion:
        groups: [Syntheses, SynthesisDetails]
