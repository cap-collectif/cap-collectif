PropositionVote:
    type: object
    config:
        interfaces: [ContributionWithAuthor]
        fields:
            author:
                type: "Person!"
                description: "The author of the contribution."
                resolve: "@=resolver('proposition_vote_author', [value])"
            value:
                type: "YesNoPairedVoteValue!"
            expired:
                type: "Boolean!"
                description: "If vote is expired, it exists but doesn't count."
            createdAt:
                type: "String!"
                description: "Date of vote."
                resolve: "@=resolver('vote_createdAt', [value])"
