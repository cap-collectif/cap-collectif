ProposalVoteOrder:
  type: input-object
  config:
    description: 'Ways in which lists of proposal votes can be ordered upon return.'
    fields:
      field:
        type: 'ProposalVoteOrderField!'
        defaultValue: 'PUBLISHED_AT'
      direction:
        type: 'OrderDirection!'
        defaultValue: 'DESC'
