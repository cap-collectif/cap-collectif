Vote:
  type: interface
  config:
    resolveType:
      '@=resolver("Capco\\AppBundle\\GraphQL\\Resolver\\Vote\\VoteTypeResolver", [value])'
    fields:
      id:
        type: 'ID!'
      createdAt:
        type: 'DateTime!'
        description: 'Date of vote.'
      author:
        type: 'User!'
        description: 'The author of the contribution.'
      related:
        type: 'Contribution'
        description: 'The contribution that was voted.'
