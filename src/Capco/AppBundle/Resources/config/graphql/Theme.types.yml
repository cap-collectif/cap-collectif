Theme:
  type: object
  config:
    description: 'A theme'
    fields:
      id:
        type: 'ID!'
      title:
        type: 'String!'
      url:
        type: 'URI!'
        resolve:
          '@=resolver("Capco\\AppBundle\\GraphQL\\Resolver\\Theme\\ThemeUrlResolver", [value])'
