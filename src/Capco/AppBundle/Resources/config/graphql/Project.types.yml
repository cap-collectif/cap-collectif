Project:
    type: object
    config:
        interfaces: [Node]
        description: "A participatory project."
        fields:
            id:
              type: "ID!"
            steps:
              type: "[Step!]!"
              resolve: "@=resolver('project_steps', [value])"
            title:
              type: "String!"
