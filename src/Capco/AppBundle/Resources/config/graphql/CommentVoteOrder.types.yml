CommentVoteOrder:
    type: input-object
    config:
        description: "Ways in which lists of comment votes can be ordered upon return."
        fields:
            field:
                type: "CommentVoteOrderField!"
                defaultValue: "CREATED_AT"
            direction:
                type: "OrderDirection!"
                defaultValue: "DESC"
