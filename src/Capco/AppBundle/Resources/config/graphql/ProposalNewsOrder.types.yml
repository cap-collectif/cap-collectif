ProposalNewsOrder:
    type: input-object
    config:
        description: "Ways in which lists of posts can be ordered upon return."
        fields:
            field:
                type: "ProposalNewsOrderField!"
                defaultValue: "CREATED_AT"
            direction:
                type: "OrderDirection!"
                defaultValue: "DESC"
