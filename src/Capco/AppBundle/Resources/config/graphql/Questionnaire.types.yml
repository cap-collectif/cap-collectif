Questionnaire:
    type: object
    config:
        interfaces: [Node]
        fields:
            id:
                type: "ID!"
            title:
                type: "String!"
            # order by position
            questions:
                type: "[Question!]!"
                resolve: "@=resolver('questionnaire_questions', [value])"
