ChangeProposalNotationInput:
  type: relay-mutation-input
  config:
    fields:
      proposalId:
        type: 'ID!'
        description: 'The proposal id'
      estimation:
        type: 'Int'
      likers:
        type: '[ID!]'
