InternalContributionOrder:
  type: input-object
  config:
    name: ContributionOrder
    description: 'Ways in which lists of contributions can be ordered upon return.'
    fields:
      field:
        type: 'InternalContributionOrderField!'
        defaultValue: 'POSITION'
      direction:
        type: 'OrderDirection!'
        defaultValue: 'DESC'
