PrivatableVote:
  type: interface
  config:
    resolveType: '@=resolver("Capco\\AppBundle\\GraphQL\\Resolver\\Vote\\VoteTypeResolver", [value])'
    fields:
      private:
        # TODO This should probably be non-null
        type: 'Boolean'
        description: 'Whether the vote is anonymous or not.'
