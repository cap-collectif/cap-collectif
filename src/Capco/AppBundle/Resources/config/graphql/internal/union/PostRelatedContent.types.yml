PostRelatedContent:
  type: union
  config:
    description: 'A related content for a post'
    types: [Theme, InternalProposal, InternalProject]
    resolveType: '@=resolver("Capco\\AppBundle\\GraphQL\\Resolver\\Post\\PostRelatedContentTypeResolver", [value])'
