ProposalVoteConnection:
  type: relay-connection
  config:
    nodeType: InternalProposalVote!
    connectionFields:
      totalCount:
        type: Int!
      creditsLeft:
        type: Int
      creditsSpent:
        type: Int
