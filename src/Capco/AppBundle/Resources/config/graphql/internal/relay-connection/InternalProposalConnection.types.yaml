InternalProposalConnection:
  type: relay-connection
  config:
    name: ProposalConnection
    nodeType: InternalProposal!
    connectionFields:
      totalCount:
        type: Int!
      fusionCount:
        type: Int!
