ProposalRevisionConnection:
    type: relay-connection
    config:
        nodeType: InternalProposalRevision!
        connectionFields:
            totalCount:
                type: Int!
                description: Identifies the total count of items in the connection.
