ParticipantConnection:
    type: relay-connection
    config:
        nodeType: InternalParticipant!
        connectionFields:
            totalCount:
                type: Int!
                description: Identifies the total count of items in the connection.
