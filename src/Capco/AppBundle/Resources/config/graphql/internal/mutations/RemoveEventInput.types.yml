RemoveEventInput:
  type: relay-mutation-input
  config:
    name: RemoveEventInput
    description: 'Remove an event'
    fields:
      eventId:
        type: 'ID!'
        description: 'ID of an Event to delete.'
