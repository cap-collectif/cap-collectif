AddUserConnectionAttemptPayload:
  type: relay-mutation-payload
  config:
    fields:
      userConnectionAttempt:
        type: 'UserConnectionAttempt'
        description: 'The newly created connection attempt.'
