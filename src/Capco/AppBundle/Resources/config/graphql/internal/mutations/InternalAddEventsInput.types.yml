InternalAddEventsInput:
  type: relay-mutation-input
  config:
    description: 'Add many events.'
    name: AddEventsInput
    fields:
      events:
        type: '[InternalEventInput!]!'
        description: 'A list of events input to import.'
      dryRun:
        type: 'Boolean!'
        description: 'This option is useful if you want to test your import. Set it to true if you want to import definitively.'
