AddProposalsToStepsInput:
  type: relay-mutation-input
  config:
    fields:
      proposalIds:
        type: '[ID!]!'
        description: 'The ids of the proposals to add to steps'
      stepsIds:
        type: '[ID!]!'
        description: 'The ids of the steps to which proposals should be moved'
