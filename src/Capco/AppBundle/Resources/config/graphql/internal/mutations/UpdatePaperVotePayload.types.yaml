UpdatePaperVotePayload:
    type: relay-mutation-payload
    config:
        description: 'Payload of UpdatePaperVote mutation.'
        fields:
            proposal:
                type: 'InternalProposal'
                description: 'If successful, the updated proposal.'
            error:
                type: 'UpdatePaperVoteErrorCode'
                description: 'The error, if any.'
