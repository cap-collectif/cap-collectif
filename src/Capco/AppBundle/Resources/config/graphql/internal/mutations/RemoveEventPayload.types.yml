RemoveEventPayload:
  type: relay-mutation-payload
  config:
    name: RemoveEventPayload
    description: 'Remove an event'
    fields:
      deletedEventId:
        type: 'ID'
        description: 'ID of Event to deleted.'
