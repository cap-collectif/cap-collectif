CreateProjectInput:
  type: relay-mutation-input
  config:
    description: 'Create a project'
    fields:
      title:
        type: 'String!'
        description: "The project's title"
      authors:
        type: '[String!]!'
        description: "The project's authors"
      usage:
        type: 'String!'
        description: "The project's usage"
      type:
        type: 'String!'
        description: "The project's type"
