CreateProposalPayload:
  type: relay-mutation-payload
  config:
    fields:
      proposal:
        type: 'Proposal'
        description: 'The created proposal, if everything goes well !'
      userErrors:
        type: '[UserError!]'
        description: 'An unsuccessful mutation will return one or more `UserError` objects.'

