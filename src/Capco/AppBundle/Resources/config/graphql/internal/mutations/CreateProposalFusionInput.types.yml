CreateProposalFusionInput:
  type: relay-mutation-input
  config:
    description: ''
    fields:
      fromProposals:
        type: '[ID!]!'
        description: 'Proposal ids from which the fusion is created'
