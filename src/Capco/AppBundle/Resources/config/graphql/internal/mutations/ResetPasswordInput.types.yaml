ResetPasswordInput:
    type: relay-mutation-input
    config:
        description: 'Input of resetPasswordMutationInput mutation'
        fields:
            token:
                type: 'String!'
                description: 'The token needed to identify the user'
            password:
                type: 'String!'
                description: 'The new password'
