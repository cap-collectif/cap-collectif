DistrictOrderField:
  type: enum
  config:
    description: 'Ordering options for districts returned from the connection.'
    values:
      ALPHABETICAL:
        value: 'ALPHABETICAL'
      CREATED_AT:
        value: 'CREATED_AT'
