VoteOrderField:
  type: enum
  config:
    description: 'Ordering options for votes returned from the connection.'
    values:
      PUBLISHED_AT:
        value: 'PUBLISHED_AT'
      POSITION:
        value: 'POSITION'
