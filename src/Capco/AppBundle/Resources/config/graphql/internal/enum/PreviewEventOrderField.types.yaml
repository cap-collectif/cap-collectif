PreviewEventOrderField:
    type: enum
    inherits:
        - InternalEventOrderField
    config:
        name: EventOrderField
        description: 'Ordering options for events returned from the connection.'
