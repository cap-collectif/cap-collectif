SectionQuestion:
  type: object
  inherits:
    - InternalQuestion
  config:
    interfaces:
      - InternalQuestion
    description: 'A section is just a title in a quiz without any possible answer.'
    fields:
      kind:
        resolve: 'section'
