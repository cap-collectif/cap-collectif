SourceVote:
  type: object
  inherits:
    - Vote
    - Publishable
  config:
    interfaces:
      - Vote
      - Publishable
    fields:
      kind:
        type: 'String!'
        description: "Returns 'sourceVote'."
        resolve: 'sourceVote'
