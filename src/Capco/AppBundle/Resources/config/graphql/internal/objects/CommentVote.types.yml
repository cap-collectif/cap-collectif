CommentVote:
  type: object
  inherits:
    - InternalVote
    - Publishable
  config:
    interfaces:
      - Node
      - InternalVote
      - Publishable
    fields:
      contribution:
        type: 'Comment!'
        resolve: '@=value.getComment()'
      kind:
        type: 'String!'
        description: "Returns 'commentVote'."
