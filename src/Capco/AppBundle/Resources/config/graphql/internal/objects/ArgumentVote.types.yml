ArgumentVote:
  type: object
  inherits:
    - InternalVote
    - Publishable
  config:
    interfaces:
      - InternalVote
      - Publishable
    fields:
      related:
        type: 'Argument'
        description: 'Return the related contribution if the contribution is related to another.'
