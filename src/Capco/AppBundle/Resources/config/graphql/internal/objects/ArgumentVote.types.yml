ArgumentVote:
  type: object
  inherits:
    - Vote
    - Publishable
  config:
    interfaces:
      - Vote
      - Publishable
    fields:
      related:
        type: 'Argument'
        description: 'Return the related contribution if the contribution is related to another.'
