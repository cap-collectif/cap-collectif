UpdateProfilePublicDataPayload:
    type: relay-mutation-payload
    config:
        fields:
            user:
                type: "User!"
                description: "the user just have been updated"
