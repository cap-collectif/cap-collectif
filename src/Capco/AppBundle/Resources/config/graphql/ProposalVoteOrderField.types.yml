ProposalVoteOrderField:
    type: enum
    config:
        description: "Ordering options for proposals votes connections."
        values:
            CREATED_AT:
                value: "CREATED_AT"
