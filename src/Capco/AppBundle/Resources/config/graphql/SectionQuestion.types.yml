SectionQuestion:
  type: object
  inherits: [Question]
  config:
    interfaces: [Question]
    description: 'A section is just a title in a quiz without any possible answer.'
    fields:
      kind:
        resolve: 'section'
