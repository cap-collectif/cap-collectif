Argument:
    type: object
    inherits: [Node, Contribution, TrashableContribution, ContributionWithAuthor, EditableContribution]
    config:
        description: "An argument"
        interfaces: [Node, Contribution, TrashableContribution, ContributionWithAuthor, EditableContribution]
        fields:
            createdAt:
                type: "DateTime!"
                description: "Identifies the date and time when the object was created."
            show_url:
                resolve: "@=resolver('argument_url', [value])"
            url:
                resolve: "@=resolver('argument_url', [value])"
            expired:
                type: "Boolean!"
            published:
                type: "Boolean!"
            type:
                type: "ArgumentValue!"
                description: "The type."
            votesCount:
                type: "Int!"
            body:
                type: "String!"
                description: "The content of the argument."
            kind:
                description: "argument"
