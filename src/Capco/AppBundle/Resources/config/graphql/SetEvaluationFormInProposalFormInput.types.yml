SetEvaluationFormInProposalFormInput:
    type: relay-mutation-input
    config:
        description: "Set an Evaluation form in a proposal form."
        fields:
            proposalFormId:
                type: "ID!"
            evaluationFormId:
                type: "String" # Because in the entity : Id is typed as integer
