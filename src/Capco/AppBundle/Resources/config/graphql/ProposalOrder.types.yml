ProposalOrder:
    type: input-object
    config:
        description: "Ways in which lists of proposals can be ordered upon return."
        fields:
            field:
                type: "ProposalOrderField!"
                defaultValue: "CREATED_AT"
            direction:
                type: "OrderDirection!"
                defaultValue: "DESC"
