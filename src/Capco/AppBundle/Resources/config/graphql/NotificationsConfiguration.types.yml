NotificationsConfiguration:
    type: object
    config:
        description: "A notifications configuration"
        fields:
            onCreate:
              type: "Boolean!"
            onUpdate:
                type: "Boolean!"
            onDelete:
                type: "Boolean!"
