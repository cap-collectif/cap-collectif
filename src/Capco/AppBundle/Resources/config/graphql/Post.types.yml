Post:
    type: object
    config:
        fields:
            id:
              type: "String!"
            title:
               type: "String!"
            abstract:
               type: "String!"
               resolve: "@resolver('post_abstract', '[value]')"
