CommentVote:
  type: object
  inherits:
    - Vote
    - Publishable
  config:
    interfaces:
      - Node
      - Vote
      - Publishable
    fields:
      contribution:
        type: 'Comment!'
        resolve: '@=value.getComment()'
