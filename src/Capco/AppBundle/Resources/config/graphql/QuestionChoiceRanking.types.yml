QuestionChoiceRanking:
  type: object
  config:
    description: 'The choice ranking'
    fields:
      position:
        type: 'Int!'
        description: 'The position of the choice in the ranking'
      answerCount:
        type: 'Int!'
        description: 'Return the amount of people who put the choice at the current position'
