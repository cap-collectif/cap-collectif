QuestionChoiceRanking:
  type: object
  config:
    description: 'The choice ranking'
    fields:
      position:
        type: 'Int!'
        description: 'Return the position of the choice'
      responses:
        type: 'ResponseConnection!'
        argsBuilder: 'Relay::Connection'
        resolve: '@=resolver("Capco\\AppBundle\\GraphQL\\Resolver\\QuestionChoice\\QuestionChoiceRankingResponseResolver", [value, args])'
        description: 'Select the responses of the c'
