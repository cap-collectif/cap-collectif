SubscriptionTypeValue:
  type: enum
  config:
    description: '3 possible values for a subscription.'
    values:
      ALL:
        value: 'ALL'
      ESSENTIAL:
        value: 'ESSENTIAL'
      MINIMAL:
        value: 'MINIMAL'