PreviewChangeEventInput:
  type: relay-mutation-input
  inherits:
    - PreviewAddEventInput
  config:
    name: ChangeEventInput
    description: 'Input type of changeEvent'
    fields:
      id:
        type: 'ID!'
        description: 'The ID of the Event to modify.'
