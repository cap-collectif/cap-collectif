PreviewProjectOrderField:
  type: enum
  config:
    name: ProjectOrderField
    description: 'Ordering projects.'
    values:
      POPULAR:
        value: 'POPULAR'
        description: 'Allows ordering a list of projects by there number of contributions.'
      LATEST:
        value: 'LATEST'
        description: 'Allows ordering a list of projects by when they were published.'
