PreviewEventConnection:
  type: relay-connection
  config:
    name: EventConnection
    nodeType: PreviewEvent!
    connectionFields:
      totalCount:
        type: Int!
        description: Identifies the total count of items in the connection.
