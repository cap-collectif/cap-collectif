EditableContribution:
    type: interface
    config:
        description: "A contribution with an author"
        fields:
            updatedAt:
                type: "String!"
                description: "Date of last edition."
