ReportingOrder:
  type: input-object
  config:
    description: 'Ways in which lists of reportings can be ordered upon return.'
    fields:
      field:
        type: 'ReportingOrderField!'
        defaultValue: 'CREATED_AT'
      direction:
        type: 'OrderDirection!'
        defaultValue: 'DESC'
