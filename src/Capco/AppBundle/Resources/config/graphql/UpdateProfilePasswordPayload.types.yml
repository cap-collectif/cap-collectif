UpdateProfilePasswordPayload:
  type: relay-mutation-payload
  config:
    fields:
      user:
        type: 'User!'
        description: 'The user who just have been updated'
      error:
        type: 'String'
        description: 'The error'
