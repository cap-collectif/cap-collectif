ArgumentOrder:
  type: input-object
  config:
    description: 'Ways in which argument connections can be ordered.'
    fields:
      field:
        type: 'ArgumentOrderField!'
        description: 'The field in which to order nodes by.'
      direction:
        type: 'OrderDirection!'
        defaultValue: 'ASC'
        description: 'The direction in which to order nodes.'
