Follower:
    type: object
    config:
        description: "A Follower"
        fields:
            id:
              type: "ID!"
            user:
              type: "User!"
            proposal:
              type: "Proposal!"
