services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  Capco\AppBundle\Processor\:
    resource: '%kernel.root_dir%/../src/Capco/AppBundle/Processor/*'

  user_archive_request.processor:
    class: Capco\AppBundle\Processor\UserArchive\UserArchiveRequestProcessor
    autowire: true
    arguments:
      $rootDir: '%kernel.root_dir%'
