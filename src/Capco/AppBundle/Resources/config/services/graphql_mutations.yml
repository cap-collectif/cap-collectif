services:
  global_id_resolver:
      class: Capco\AppBundle\GraphQL\Resolver\GlobalIdResolver
      arguments:
          - "@service_container"
      # tags:
      #     - { name: overblog_graphql.resolver }
