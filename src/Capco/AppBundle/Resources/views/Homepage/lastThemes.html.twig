{% if topics|length > 0 %}
    <div class="row">
        {% for theme in topics %}
            {% include '@CapcoApp/Theme/blockTheme.html.twig' %}
        {% endfor %}
    </div>
    <a href="{{ path('app_theme') }}" class="btn  btn-primary  btn--outline">{{ 'theme.see_all'|trans({}, 'CapcoAppBundle') }}</a>
{% endif %}
