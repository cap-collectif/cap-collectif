<div class="container">
    <h2>{% trans %}Ideas{% endtrans %}</h2>
    <div class="row">
        {% for idea in ideas %}
            {% include '@CapcoApp/Idea/lastIdeas.html.twig' %}
        {% endfor %}
    </div>
    <a href="{{ path('app_idea') }}" class="btn  btn-primary  btn--outline">{{ 'idea.show.views'|trans({}, 'CapcoAppBundle') }}</a>
</div>
