{% if count > 0 %}
    <div
        id="render-projects-list"
        data-projects='{{ projects | escape }}'
    >
    </div>
    <a href="{{ path('app_project') }}" class="btn  btn-primary  btn--outline">{{ 'project.see_all'|trans({}, 'CapcoAppBundle') }}</a>
{% else %}
    <p>{{ 'project.none'|trans({}, 'CapcoAppBundle') }}</p>
{% endif %}
