{% extends '@CapcoApp/Project/base.html.twig' %}

{% block canonical_link %}{{ url('app_project_show_step', {'projectSlug': project.slug, 'stepSlug': currentStep.slug}) }}{% endblock %}

{% block metaTags %}
    {% set title = currentStep.title ~ ' ' ~ 'of.project' | trans({}, 'CapcoAppBundle') ~ ' ' ~ project.title %}
    {% set image %}{% if project.Cover %}{% path project.Cover, 'blockHeader' %}{% endif %}{% endset %}
    {% include 'CapcoAppBundle:Default:meta.html.twig' with {
        'title':       title,
        'type':        'project',
        'description': currentStep.metaDescription ?: currentStep.body|striptags|truncate(250, true),
        'author':      is_feature_enabled('profiles') ? url('capco_user_profile_show_all', {'slug': project.Author.slug}) : null,
        'image':       image,
        'url':         url('app_project_show_step', {'projectSlug': project.slug, 'stepSlug': currentStep.slug}),
    } only %}
{% endblock %}

{% block title %}
    {{ parent() }} - {{ currentStep.title }}
{% endblock %}

{% block jumbotron %}{% endblock %}

{% block details %}
    <h2 class="h2">{{ currentStep.title }}</h2>
    {% if currentStep.startAt %}
        <div class="mb-30">
            <i class="cap cap-calendar-2-1"></i>
            {% if currentStep.endAt %}
                {{ 'global.dates.full.days' | trans({
                    '{startD}': currentStep.startAt | localizeddate('long', 'none', app.request.locale),
                    '{startT}': currentStep.startAt | localizeddate('none', 'short', app.request.locale),
                    '{endD}': currentStep.endAt | localizeddate('long', 'none', app.request.locale),
                    '{endT}': currentStep.endAt | localizeddate('none', 'short', app.request.locale)
                }) }}
            {% else %}
                {{ 'global.dates.full_day' | trans({
                    '{date}': currentStep.startAt | localizeddate('long', 'none', app.request.locale),
                    '{time}': currentStep.startAt |localizeddate('none', 'short', app.request.locale)
                }) }}
            {% endif %}
        </div>
    {% endif %}
    <div class="block  block--bordered  box">
        {{ currentStep.body | raw }}
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {% if currentStep.customCode is defined and currentStep.customCode != '' %}{{ currentStep.customCode | raw }}{% endif %}
{% endblock %}
