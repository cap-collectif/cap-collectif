{% extends '@CapcoApp/Project/layout_with_widget.html.twig' %}

{% block canonical_link %}{{ '' }}{% endblock %}

{% block title %}
    {{ parent() }} - {{ proposal.title }}
{% endblock %}

{% block jumbotron %}{% endblock %}

{% block reduxStore %}
  {% set initialState = {
    'proposal': {
      'currentProposalById': proposal.id,
      'proposals': {
        (proposal.id) : proposalSerialized
      }
    }
  } %}
  {% include '@CapcoApp/redux_store.html.twig' with {'data' : state|merge(initialState)} %}
{% endblock %}

{% block body %}
    {{ react_intl_component('ProposalPageApp', {
        'props': props,
        'rendering': (app.user and not feature_is_active('server_side_rendering')) ? 'client_side' : 'both',
      })
    }}
{% endblock %}
