{% block body_text %}
    {% autoescape false %}
        {{ 'proposal_status_change_selection.notification.body'|trans(
            {
                '%project%': selection.proposal.step.project.title,
                '%proposal%': selection.proposal.title,
                '%step%': selection.selectionStep.title,
                '%status%': selection.status.name,
                '%proposal_link%': url('app_project_show_proposal', {'projectSlug': selection.proposal.step.project.slug, 'stepSlug': selection.proposal.step.slug, 'proposalSlug': selection.proposal.slug}),
                '%user%': selection.proposal.author.getDisplayName(),
            },
            'CapcoAppBundle',
            user_locale
        ) | raw }}
    {% endautoescape %}
{% endblock %}
{% block body_html %}{% endblock %}
