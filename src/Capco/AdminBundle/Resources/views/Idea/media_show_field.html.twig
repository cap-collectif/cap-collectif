{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    {%  if value is not null %}
        {% thumbnail value, 'small' with {'title': value.name, 'alt': value.name} %}
    {%  endif %}
{% endblock %}