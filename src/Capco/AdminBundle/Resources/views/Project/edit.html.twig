{% extends 'SonataAdminBundle:CRUD:base_edit.html.twig' %}

{% use 'CapcoAdminBundle:Project:base_edit_form.html.twig' with form as parentForm %}
{% block sonata_pre_fieldsets %}
    {% include '@CapcoApp/redux_store.html.twig' %}
    {{ react_render_component('ProjectAdminAppDeprecated', {	
        'props': {	
            'projectId': object.id|toGlobalId('Project')	
        }	
    }) }}	
    {# 
       We still need to restore external projects block
          
    {{ react_render_component('ProjectExternalProjectAdminApp', {	
        'props': {	
            'projectId': object.id|toGlobalId('Project'),	
            'hostUrl' : app.request.httpHost,	
        }	
    }) }} #}
{% endblock %}
