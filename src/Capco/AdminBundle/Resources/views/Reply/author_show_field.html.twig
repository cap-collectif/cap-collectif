{% extends '@SonataAdmin/CRUD/base_show_field.html.twig' %}

{% block field %}
    {% apply spaceless %}
        {% if value %}
            <a href="{{ path('admin_capco_user_user_edit', {id: value.id}) }}">
                {{ value.displayName | trans({}, 'CapcoAppBundle') }}
            </a>
        {% endif %}
    {% endapply %}
{% endblock %}
