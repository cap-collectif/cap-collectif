{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    {%  if value is not null %}
        <img
            title={{ value.name }}
            alt={{ value.name }}
            src={{ media_public_url(uvalue, 'default_form') }}
        />
    {%  endif %}
{% endblock %}
