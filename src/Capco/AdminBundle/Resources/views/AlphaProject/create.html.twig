{% extends 'SonataAdminBundle:CRUD:base_edit.html.twig' %}

{% block sonata_page_content %}
    {% include '@CapcoApp/redux_store.html.twig' %}
    {{ react_render_component('ProjectAdminApp', {
        'props': {
            'projectId': object.id is not null ? object.id|toGlobalId('Project') : null,
            'firstCollectStepId': (object.id is not null and object.firstCollectStep is not null) ? object.firstCollectStep.id|toGlobalId('CollectStep') : null
        }
    }) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
