{% extends admin.templateRegistry.getTemplate('base_list_field') %}

{% block field %}
    {% apply spaceless %}
        {% if value %}
            <span class="label label-{{ value.color }}">{{ value.name }}</span>
        {% endif %}
    {% endapply %}
{% endblock %}
