{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% spaceless %}
        {% if value %}
            <a href="{{ path('admin_capco_user_user_edit', {id: value.id}) }}">{{ value.displayName }}</a>
        {% endif %}
    {% endspaceless %}
{% endblock %}
