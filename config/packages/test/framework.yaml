framework:
    router:
        resource: '%kernel.project_dir%/config/routes/routing.yaml'
    test: ~
    profiler:
        collect: false
    translator:
        enabled: true
    http_client:
        scoped_clients:
