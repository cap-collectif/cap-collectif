framework:
    router:
        resource: '%kernel.project_dir%/config/routes/routing.yaml'
    test: ~
    profiler:
        collect: false
    translator:
        enabled: true
    http_client:
        scoped_clients:
            tipsmeee.client:
                base_uri: 'https://api-stage.tipsmeee.com/'
