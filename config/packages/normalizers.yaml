services:
    _defaults:
        autowire: true
        # public: false

    Capco\AppBundle\Normalizer\:
        resource: '%kernel.project_dir%/src/Capco/AppBundle/Normalizer/'
        tags: ['serializer.normalizer']
