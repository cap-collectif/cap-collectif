;; This file is the same as the one in infrastructure/local/php-fpm/capco.ini but allows Symfony binary
;; to use this config. It must be at the root of the project
;; Specific config for capco
memory_limit=512M
date.timezone=Europe/Paris
upload_max_filesize=20M
post_max_size=20M
