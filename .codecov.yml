codecov:
  allow_coverage_offsets: true
  notify:
    require_ci_to_pass: yes
    
coverage:
  round: down
  precision: 2
  status:
    project:
      default:
        threshold: 90
      # frontend:
      #   target: 89%
      #   flags: frontend
      # backend:
      #   target: 50%
      #   flags: backend
    patch:
      default:
        threshold: 90
# flags:
#   backend:
#     paths:
#       - src/
#   frontend:
#     paths:
#       - app/Resources/js/
