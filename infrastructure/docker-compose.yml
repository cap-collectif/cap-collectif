builder:
    build: services/builder
    volumes:
        - ../:/capco

demo:
    build: services/web
    environment:
        SHORTNAME: demo
    ports:
        - "1080:80"
    expose:
        - "3306"
