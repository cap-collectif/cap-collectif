application:
    environment:
        DOCKER_GID: 100
    volumes:
        - ../../:/var/www
        - /var/run/docker.sock:/var/run/docker.sock
        - /usr/local/bin/docker:/usr/bin/docker

database:
    ports:
        - "3306:3306"

chrome:
   ports:
    - "5900:5900"

# You can also test on a different browser
# firefox:
#   image: selenium/node-firefox-debug:latest
#   links:
#     - seleniumhub:hub
#     - application:application
#   ports:
#     - "5901:5900"

# Used to generate spyl94/capco-fixtures
databasefixtures:
    build: ../services/databasefixtures
