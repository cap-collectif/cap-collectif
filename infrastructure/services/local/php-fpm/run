#!/bin/bash

chown -R capco:capco /var/www/var

echo "Checking if mysql is booted..."
while ! mysql --host=database -uroot > /dev/null 2>&1;
  do sleep 1
  echo "Waiting mysql to boot..."
done
echo "Mysql is ready!"

exec php5-fpm -y /etc/service/php-fpm/fpm.conf -O
