#!/bin/bash

# First boot
if [ ! -f /var/lib/mysql/ibdata1 ]; then
  mkdir /var/lib/mysql
  cp -R /var/lib/mysql_save/* /var/lib/mysql/
  chown -R mysql:mysql /var/lib/mysql
fi

exec mysqld_safe --log-error=mysqld_safe_error.log  --tc-heuristic-recover=ROLLBACK
